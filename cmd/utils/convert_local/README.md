# Convert an archive locally

## Usage
  ```shell
  $  go run cmd/utils/convert_local/main.go --archive=[path/to/archive] \
                                            --output=[path/to/output] \
                                            --collector=[collector name]
  ```